# 積み残し

コラム運用の未決着リスト。片付いたら消す。

## 判断待ち

### 1. 公開済み記事 9190 がマニュアル違反

| 項目 | ルール | 9190の実態 |
|---|---|---|
| タイトル文字数 | 32字以内 | **44字** |
| タイトルの区切り | 前半／後半を `！` `？` で区切る | **区切りなし** |
| 最初のH2 | タイトル前半と同じ意味・同じKW | 「E-E-A-Tとは何か——」で**不一致** |
| H2の数 | 3,000字ならまとめ含め4本まで | 本文3,242字に対し**8本** |

まとめが最後のH2である点だけ合っている。

タイトルを直すとアイキャッチも作り直しになる。ただし**サムネ側の文言
「誰が書いたか、見えていますか？」が既に前半＋？の形をしている**ので、
そこからタイトルを再構成すれば両方揃う。→ やるかどうかの判断待ち。

### 2. 装飾ルール（JIN:R）

旧JINの `concept-box6` `[box03]` `box21` が動かない。現行コラムはインラインstyleの独自ボックスで、
統一記法から外れている。**放置すると記事ごとに見た目がばらつく。**

### 3. 下書きの整理

- **9295** … タイトルが空のまま残っている
- **9236** … 公開済みの9268と内容が重複している可能性（どちらも売上管理表テンプレート）

## 着手待ち

### 4. アイキャッチ素材の10〜18番

母艦（Canva `DAF2Hmjvfm4`）を読み直して `docs/EYECATCH-ASSETS.md` を埋める。

### 5. 記事1本目（9327）の仕上げ

「口コミ返信の例文つき！低評価レビューで店の印象を落とさない書き方」は下書き済み（本文3,562字、
マニュアル準拠チェック済み）。**アイキャッチ未作成。**

作業用コピー `DAHTTu8e0Lg` は母艦から複製したままで、文言が元ページ（Easy Watermark）のまま残っている。
3行構成に沿って差し替える。

なお本文に**現場のエピソードが入っていない**。実績を捏造しないため空けてある。
`docs/WRITING-STYLE.md` §7の必須要件を満たすには、具体の数字か現場で見たことを1つ足す必要がある。

### 6. SNS自動投稿プラグイン

WordPress公式の現行データ（2026-08時点）。

| プラグイン | 有効数 | X | Threads | 最終更新 |
|---|---|---|---|---|
| **Blog2Social** | 50,000 | ○ | ○ | 2026-08-20 |
| Bit Social | 6,000 | ○ | ○ | 2026-08-19 |
| WP to Buffer | 7,000 | ○ | ○ | 2026-08-24 |
| Jetpack | 3,000,000 | **✗** | ○ | 2026-08-20 |

**Jetpackは対象外**（XがAPI有料化以降サポート終了のまま）。**Blog2Socialを推奨** ——
X・Threads両対応、導入実績が最多、更新も直近、予約投稿と連携できる。

接続時に**X側のAPIプランごとの投稿数上限を実際に確認すること**。1日3投稿なら月90件で
通常は無料枠に収まるはずだが、断言はできない。

### 7. テーマの追加

1日3本＝月90本。いま20本なので**7日で尽きる**。既存61本からの派生はほぼ汲み尽くしているので、
次は別の切り口（クライアントワークの実例、業種別、季節もの）で足す。

## 環境の制約

- **サーチコンソールのコネクタは提供されていない。** データが要るときは管理画面からCSVを書き出す（月1回で足りる）
- **Canvaのストック素材ライブラリは検索できない。** コネクタに該当ツールが無いためで、アカウントの制限ではない。
  外部の無料ライセンス素材をCanvaにアップロードする経路は確立済み
- **クラウド側のセッションからは gouter.works に接続できない**（egressプロキシが遮断）。
  WordPressへの反映・プレビュー・SERP確認は、サイトに到達できる環境から行う
